# -*- sh -*-
# Xsession.d script to start gpdrotate
#
# This file is sourced by Xsession(5), not executed.

. /etc/gpd/rotate.conf
exec /usr/local/sbin/gpd-rotate --display=${DISPLAY} --touchscreen=${TOUCHSCREEN} &